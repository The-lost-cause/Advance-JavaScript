// Semicolon is OPTIONAL in Js

let myName = 'Karan'
console.log(myName)

const firstName = 'Harleen'
const lastName = 'Dang'
const sentence = 'Hello ' + firstName + " " + '! How are you?'
console.log(sentence)

// Better way will be using BACKTICKS ` (Template Strings)

const newSentence = `Hello ${lastName}! How are you?`
console.log(newSentence)

console.log(Number.MAX_SAFE_INTEGER)
console.log(Number.MIN_SAFE_INTEGER)

// Conditional Statements

const isSkyBlue = true

if (isSkyBlue) {
    console.log('The sky is blue!')
} else {
    console.log('The sky is not blue')
}

// const must always be INITALIZED Where it is declared

// const name
// name = 'Leenu'                          ERROR
// console.log(name)

